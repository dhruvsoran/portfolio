# Copy to .env and fill in your real values.
# This file IS committed to the repo (only .env is ignored).

# ─── Optional: API base URL ──────────────────────────────────────────────
# Leave blank when the API is served from the same origin (recommended).
# Set this when the client is hosted on a different domain than the server,
# e.g. https://api.dhruvsoran.com
VITE_API_URL=
